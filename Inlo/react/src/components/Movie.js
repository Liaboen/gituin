/* eslint-disable prettier/prettier */
import React from 'react';

const Movie = ({title, actor}) => (
    <div>
        <p>Filmtittel: {title}. Skuespiller: {actor}. </p>
    </div>
);

export default Movie;
